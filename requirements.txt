# requirements.txt — JetLearn Insights + Predictability
streamlit>=1.30
pandas>=2.0
numpy>=1.24
altair>=5.0
scikit-learn>=1.3
# Optional extras:
    #only if you switch back to the Poisson forecaster version
# pyarrow>=14         # speeds up CSV/Parquet I/O (optional)
